version: '3.8'

services:
  dredge:
    build: .
    container_name: dredge
    ports:
      - "8000:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./tests:/app/tests
    environment:
      - PYTHONUNBUFFERED=1
