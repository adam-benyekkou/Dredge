# Volumes Management

Docker volumes persist data generated by containers. Dredge helps identify "dangling" volumes—those not attached to any running container.

*   **Active**: Volume is mounted to a running container.
*   **Dangling**: Volume is not in use. Candidate for pruning.
