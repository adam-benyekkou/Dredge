# Cleanup Policies

Automate your registry hygiene with policies.

*   **Keep Count**: Ensure the last N tags are always kept (e.g., keep last 5 deployments).
*   **Max Age**: Delete images older than X days (e.g., delete > 30 days).
*   **Whitelist**: Regex patterns to exclude from cleanup (e.g., `^prod-.*`).
