[project]
name = "dredge"
version = "0.1.0"
description = "Docker FinOps & Lifecycle Management Tool"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "taskiq",
    "sqlmodel",
    "jinja2",
    "docker",
]
